@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<Stylist>

<h1>Stylists</h1>


@if (Model != null && Model.Any())
{
  @foreach (var stylist in Model)
  {
    <div id="boxed">
      <h4># @stylist.StylistID - <a href="/Stylist/@stylist.StylistID">@stylist.StylistName</a> -
        @stylist.StylistDescription</h4>
    </div>
  }
}
else
{
  <h4 id="boxed">no stylists yet</h4>
}


<h4 id="boxed"><a href='/stylist/new'>new stylist</a><br><a href='/menu'>main menu</a></h4>
